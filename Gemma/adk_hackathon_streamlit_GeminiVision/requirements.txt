# ─────────────────────────────────────────────────────────────────────────────
# Emakia hackathon — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Tested with Python 3.11 (recommended).
# Strategy: pin only what causes conflicts; let pip resolve the rest.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core web app ─────────────────────────────────────────────────────────────
streamlit>=1.39,<2.0
python-dotenv>=1.0.1

# ── Data handling ────────────────────────────────────────────────────────────
pandas>=2.2,<3.0
db-dtypes>=1.3.0

# ── Tabular file format readers (for arbitrary Kaggle datasets) ──────────────
# pandas.read_excel needs openpyxl; read_parquet needs pyarrow.
# Without these, Excel/Parquet datasets will fail to load.
openpyxl>=3.1.0
pyarrow>=15.0.0

# ── Google AI Studio (cloud Gemma 4 backend) ─────────────────────────────────
google-generativeai>=0.8.3
google-genai>=0.3.0

# ── Google ADK (multi-agent pipeline) ────────────────────────────────────────
google-adk>=1.30,<2.0

# ── Local Gemma 4 backend (llama.cpp + GGUF) ─────────────────────────────────
# CRITICAL for the Safety & Trust + llama.cpp tracks.
# On Apple Silicon, install separately FIRST with Metal (see bottom).
llama-cpp-python>=0.3.1
huggingface_hub>=0.26.0

# ── Kaggle dataset access ────────────────────────────────────────────────────
kaggle>=1.6.17

# ── Reddit + HTML parsing ────────────────────────────────────────────────────
praw>=7.8.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# ── Media handling ───────────────────────────────────────────────────────────
Pillow>=10.4,<11
yt-dlp>=2024.10.22

# ── TOML parser (used by tools/gemini_vision_with_key.py) ────────────────────
toml>=0.10.2

# ─────────────────────────────────────────────────────────────────────────────
# INSTALL ORDER (Apple Silicon recommended):
#
#   pip install --upgrade pip
#   CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python --no-cache-dir
#   pip install -r requirements.txt
#
# When testing with Docker, force a fresh install after editing this file:
#
#   docker build --no-cache -t emakia-test .
# ─────────────────────────────────────────────────────────────────────────────
